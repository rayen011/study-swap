{
  "name": "studyswap-rules-tests",
  "description": "Security rules tests for Firestore and Cloud Storage.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "emulate": "firebase emulators:exec --only firestore,storage --project studyswap-rules-test \"npm test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.2.0",
    "vitest": "^3.0.5"
  }
}
